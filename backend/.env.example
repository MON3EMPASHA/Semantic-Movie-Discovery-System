# Server Configuration
NODE_ENV=development
PORT=4000

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/movie-recommendation

# TMDB API (for seeding real movies) - Get free key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Embedding Configuration (Local - no API key needed)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Vector Database Configuration (choose one)
VECTOR_DB_PROVIDER=qdrant  # Options: qdrant, pinecone, chroma

# Qdrant Configuration (Local or Cloud)
VECTOR_DB_URL=http://localhost:6333
# VECTOR_DB_API_KEY=your_qdrant_api_key  # Only needed for Qdrant Cloud

# Pinecone Configuration (uncomment if using Pinecone)
# PINECONE_API_KEY=your_pinecone_api_key
# PINECONE_INDEX=movies
# PINECONE_ENVIRONMENT=us-east-1-aws

# Vector Collection/Index Name
VECTOR_COLLECTION=movies
VECTOR_DIMENSION=384
